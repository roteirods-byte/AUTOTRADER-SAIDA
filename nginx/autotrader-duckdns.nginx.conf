limit_req_zone $binary_remote_addr zone=autotrader:10m rate=10r/s;

# MFE (8082)
server {
  server_name mfejorge.duckdns.org;

  location ^~ /.well-known/acme-challenge/ { auth_basic off; }

  # auth_basic "AUTOTRADER - ACESSO RESTRITO";  (DESLIGADO)
  # auth_basic_user_file /etc/nginx/.htpasswd;  (DESLIGADO)

  location / {
    limit_req zone=autotrader burst=30 nodelay;
    proxy_pass http://127.0.0.1:8082;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/mfejorge.duckdns.org/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/mfejorge.duckdns.org/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}

# POSICIONAL (8090)
server {
  server_name posjorge.duckdns.org;

  location ^~ /.well-known/acme-challenge/ { auth_basic off; }

    # auth_basic "AUTOTRADER - ACESSO RESTRITO";  (DESLIGADO)
    # auth_basic_user_file /etc/nginx/.htpasswd;  (DESLIGADO)
    # TOP10 POSICIONAL (proxy p/ node 8090)
    location = /top10 {
      add_header Cache-Control "no-store";
      proxy_pass http://127.0.0.1:8090/top10;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

location / {
    limit_req zone=autotrader burst=30 nodelay;
    proxy_pass http://127.0.0.1:8090;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

    listen 443 ssl; # managed by Certbot

ssl_certificate /etc/letsencrypt/live/posjorge.duckdns.org/fullchain.pem;
ssl_certificate_key /etc/letsencrypt/live/posjorge.duckdns.org/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot


    # --- POSICIONAL API (fix) ---
    location /api/entrada {
        # auth_basic off;  (DESLIGADO)
        proxy_pass http://127.0.0.1:8090/api/entrada;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        add_header Cache-Control "no-store";
    }

}

server {
    if ($host = mfejorge.duckdns.org) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


  listen 80;
  server_name mfejorge.duckdns.org;
    return 404; # managed by Certbot



    # --- POSICIONAL API (fix) ---
    location /api/entrada {
        # auth_basic off;  (DESLIGADO)
        proxy_pass http://127.0.0.1:8090/api/entrada;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        add_header Cache-Control "no-store";
    }
}
server {
    if ($host = posjorge.duckdns.org) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


  listen 80;
  server_name posjorge.duckdns.org;
    return 404; # managed by Certbot


}

# SAIDA (8081)
server {
  server_name saida1jorge.duckdns.org;

  # SAIDA API (8096)
  location ^~ /api/saida/ {
    proxy_pass http://127.0.0.1:8096;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "no-store";
  }

  # PRO API (8095) - aceita /api/pro e /api/pro/...
  location = /api/pro {
    proxy_pass http://127.0.0.1:8095/api/pro;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 10s;
    add_header Cache-Control "no-store";
  }
  location ^~ /api/pro/ {
    proxy_pass http://127.0.0.1:8095;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "no-store";
  }

  # UI (painel) - tudo que não for /api vai para 8096
  location / {
    auth_basic off;
    limit_req zone=autotrader burst=30 nodelay;
    proxy_pass http://127.0.0.1:8096;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  listen 443 ssl;
  ssl_certificate /etc/letsencrypt/live/saida1jorge.duckdns.org/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/saida1jorge.duckdns.org/privkey.pem;
  include /etc/letsencrypt/options-ssl-nginx.conf;
  ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
  if ($host = saida1jorge.duckdns.org) { return 301 https://$host$request_uri; }
  listen 80;
  server_name saida1jorge.duckdns.org;
  return 404;
}



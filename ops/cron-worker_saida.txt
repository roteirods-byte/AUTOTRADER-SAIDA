# Exemplo de cron (a cada 5 minutos)
# Opção 1 (como estava): usa runner fora do repo
*/5 * * * * root /usr/local/bin/worker_saida_run.sh

# Opção 2 (recomendada): runner dentro do repo (ajuste REPO_DIR)
#*/5 * * * * root REPO_DIR=/root/AUTOTRADER-SAIDA /root/AUTOTRADER-SAIDA/scripts/worker_saida_run.sh
